# Environment variables for the Streamlit app
# Replace placeholders with your real keys before running the app.
# DO NOT commit this file to source control (add .env to .gitignore).

# Pollinations endpoint (GET expects URL-encoded prompt appended)
POLLINATIONS_BASE_URL=https://image.pollinations.ai/prompt/

